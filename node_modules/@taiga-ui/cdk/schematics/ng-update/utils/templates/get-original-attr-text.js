"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getOriginalAttrText = getOriginalAttrText;
exports.replaceAttrValue = replaceAttrValue;
// Slices the attribute from source, preserving camelCase that parse5 lowercases in the AST.
function getOriginalAttrText(template, element, attr) {
    var _a, _b;
    const attrLocation = (_b = (_a = element.sourceCodeLocation) === null || _a === void 0 ? void 0 : _a.attrs) === null || _b === void 0 ? void 0 : _b[attr.name.toLowerCase()];
    if (attrLocation) {
        return template.slice(attrLocation.startOffset, attrLocation.endOffset);
    }
    return attr.value ? `${attr.name}="${attr.value}"` : attr.name;
}
// Replaces an attribute's value regardless of quote style (single/double/none).
function replaceAttrValue(attrText, value) {
    var _a, _b, _c, _d;
    const eqIndex = attrText.indexOf('=');
    if (eqIndex === -1) {
        return attrText;
    }
    const afterEq = attrText.slice(eqIndex + 1);
    const leadingWs = (_b = (_a = /^\s*/.exec(afterEq)) === null || _a === void 0 ? void 0 : _a[0]) !== null && _b !== void 0 ? _b : '';
    const rest = afterEq.slice(leadingWs.length);
    const trailingWs = (_d = (_c = /\s*$/.exec(rest)) === null || _c === void 0 ? void 0 : _c[0]) !== null && _d !== void 0 ? _d : '';
    const core = rest.slice(0, rest.length - trailingWs.length);
    const firstChar = core.charAt(0);
    const quote = firstChar === '"' || firstChar === "'" ? firstChar : '"';
    return `${attrText.slice(0, eqIndex)}=${leadingWs}${quote}${value}${quote}${trailingWs}`;
}
//# sourceMappingURL=get-original-attr-text.js.map