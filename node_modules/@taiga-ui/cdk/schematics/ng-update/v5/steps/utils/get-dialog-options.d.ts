import { type CallExpression, type ObjectLiteralExpression } from 'ng-morph';
export declare const TUI_DIALOG_FACTORY = "tuiDialog";
export declare const TUI_DIALOG_SERVICE = "TuiDialogService";
export declare const TUI_DIALOG_OPTIONS_PROVIDER = "tuiDialogOptionsProvider";
/**
 * Returns the dialog options object literal for a call, or `undefined` when the call is
 * not a recognized dialog entry point. Covers `tuiDialog(component, options)`,
 * `TuiDialogService.open(content, options)` and `tuiDialogOptionsProvider(options)`.
 */
export declare function getDialogOptions(call: CallExpression): ObjectLiteralExpression | undefined;
