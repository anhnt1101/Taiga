"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.migrateInputRange = migrateInputRange;
const elements_1 = require("../../../../utils/templates/elements");
const template_resource_1 = require("../../../../utils/templates/template-resource");
function migrateInputRange({ resource, recorder, fileSystem, }) {
    const template = (0, template_resource_1.getTemplateFromTemplateResource)(resource, fileSystem);
    const templateOffset = (0, template_resource_1.getTemplateOffset)(resource);
    const elements = (0, elements_1.findElementsByTagName)(template, 'tui-input-range');
    elements.forEach((element) => {
        const labels = (0, elements_1.findElementsByFn)(element.childNodes, (el) => el.tagName === 'label' && (0, elements_1.hasElementAttribute)(el, 'tuiLabel'));
        labels.forEach((label) => {
            const loc = label.sourceCodeLocation;
            if (!(loc === null || loc === void 0 ? void 0 : loc.startTag) || !(loc === null || loc === void 0 ? void 0 : loc.endTag)) {
                return;
            }
            const startTag = loc.startTag;
            const endTag = loc.endTag;
            const content = template.slice(startTag.endOffset, endTag.startOffset);
            recorder.remove(templateOffset + loc.startOffset, loc.endOffset - loc.startOffset);
            recorder.insertRight(templateOffset + loc.startOffset, content);
        });
    });
}
//# sourceMappingURL=migrate-input-range.js.map