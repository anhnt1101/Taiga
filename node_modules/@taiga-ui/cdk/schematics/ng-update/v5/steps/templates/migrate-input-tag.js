"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.migrateInputTag = migrateInputTag;
const add_import_to_closest_module_1 = require("../../../../utils/add-import-to-closest-module");
const insert_todo_1 = require("../../../../utils/insert-todo");
const elements_1 = require("../../../../utils/templates/elements");
const template_resource_1 = require("../../../../utils/templates/template-resource");
const control_state_attrs_1 = require("../../../utils/templates/control-state-attrs");
const get_original_attr_text_1 = require("../../../utils/templates/get-original-attr-text");
const remove_attr_1 = require("../../../utils/templates/remove-attr");
const replace_tag_1 = require("../../../utils/templates/replace-tag");
const migrate_hint_on_legacy_controls_1 = require("./migrate-hint-on-legacy-controls");
const DOCS_LINK = 'https://taiga-ui.dev/components/input-chip';
const HINT_ATTRS = new Set([
    '[tuiHintAppearance]',
    '[tuiHintContent]',
    '[tuiHintDirection]',
    'tuiHintAppearance',
    'tuiHintContent',
    'tuiHintDirection',
].map((name) => name.toLowerCase()));
const LABEL_OUTSIDE_ATTRS = new Set([
    '[tuiTextfieldLabelOutside]'.toLowerCase(),
    'tuiTextfieldLabelOutside'.toLowerCase(),
]);
const INPUT_ATTR_RENAMES = new Map([
    ['[maxLength]'.toLowerCase(), '[maxlength]'],
    ['[placeholder]'.toLowerCase(), '[placeholder]'],
    ['[separator]'.toLowerCase(), '[separator]'],
    ['[uniqueTags]'.toLowerCase(), '[unique]'],
    ['maxLength'.toLowerCase(), 'maxlength'],
    ['placeholder'.toLowerCase(), 'placeholder'],
    ['separator'.toLowerCase(), 'separator'],
    ['uniqueTags'.toLowerCase(), 'unique'],
]);
const TEXTFIELD_ATTR_RENAMES = new Map([
    ['[disabledItemHandler]'.toLowerCase(), '[disabledItemHandler]'],
    ['[rows]'.toLowerCase(), '[rows]'],
    ['disabledItemHandler'.toLowerCase(), 'disabledItemHandler'],
    ['rows'.toLowerCase(), 'rows'],
]);
const TODO_ATTRS = new Set([
    '(searchChange)'.toLowerCase(),
    '[(search)]'.toLowerCase(),
    '[autoColor]'.toLowerCase(),
    '[editable]'.toLowerCase(),
    '[search]'.toLowerCase(),
    '[tagValidator]'.toLowerCase(),
    'autoColor'.toLowerCase(),
    'editable'.toLowerCase(),
]);
const DROPPED_ATTRS = new Set([
    '[inputHidden]'.toLowerCase(),
    '[removable]'.toLowerCase(),
    'inputHidden'.toLowerCase(),
    'removable'.toLowerCase(),
]);
function migrateInputTag({ resource, recorder, fileSystem, }) {
    const template = (0, template_resource_1.getTemplateFromTemplateResource)(resource, fileSystem);
    const templateOffset = (0, template_resource_1.getTemplateOffset)(resource);
    const elements = (0, elements_1.findElementsByTagName)(template, 'tui-input-tag');
    elements.forEach((element) => {
        var _a, _b, _c, _d, _e, _f, _g, _h, _j, _k, _l, _m, _o;
        const sourceCodeLocation = element.sourceCodeLocation;
        if ((sourceCodeLocation === null || sourceCodeLocation === void 0 ? void 0 : sourceCodeLocation.startTag) && !sourceCodeLocation.endTag) {
            migrateSelfClosingInputTag({
                element,
                template,
                recorder,
                templateOffset,
                resource,
            });
            return;
        }
        (0, replace_tag_1.replaceTag)(recorder, sourceCodeLocation, 'tui-input-tag', 'tui-textfield', template, templateOffset);
        const { value: labelOutside, isBinding: labelOutsideIsBinding } = (0, remove_attr_1.removeAttr)(element, 'tuiTextfieldLabelOutside', recorder, templateOffset);
        const isLabelOutsideTrue = labelOutside === 'true' || (!labelOutsideIsBinding && labelOutside === '');
        const isDynamic = labelOutside !== null && !isLabelOutsideTrue && labelOutside !== 'false';
        const openTagEnd = (_b = (_a = sourceCodeLocation === null || sourceCodeLocation === void 0 ? void 0 : sourceCodeLocation.startTag) === null || _a === void 0 ? void 0 : _a.endOffset) !== null && _b !== void 0 ? _b : 0;
        const controlAttrs = [...element.attrs].filter((attr) => /formcontrol|ngmodel/.exec(attr.name.toLocaleLowerCase()));
        const inputAttrs = [...element.attrs].filter((attr) => INPUT_ATTR_RENAMES.has(attr.name.toLowerCase()));
        const textfieldAttrs = [...element.attrs].filter((attr) => TEXTFIELD_ATTR_RENAMES.has(attr.name.toLowerCase()));
        const todoAttrs = [...element.attrs].filter((attr) => TODO_ATTRS.has(attr.name.toLowerCase()));
        const droppedAttrs = [...element.attrs].filter((attr) => DROPPED_ATTRS.has(attr.name.toLowerCase()));
        const controlStateAttrs = (0, control_state_attrs_1.getControlStateAttrs)(element);
        for (const attr of [
            ...controlAttrs,
            ...inputAttrs,
            ...textfieldAttrs,
            ...todoAttrs,
            ...droppedAttrs,
        ]) {
            const { startOffset = 0, endOffset = 0 } = (_e = (_d = (_c = element.sourceCodeLocation) === null || _c === void 0 ? void 0 : _c.attrs) === null || _d === void 0 ? void 0 : _d[attr.name]) !== null && _e !== void 0 ? _e : {};
            recorder.remove(templateOffset + startOffset, endOffset - startOffset);
        }
        (0, control_state_attrs_1.removeControlStateAttrs)(recorder, templateOffset, element, template, controlStateAttrs);
        const textfieldAttrStr = textfieldAttrs.reduce((result, attr) => {
            var _a;
            const name = (_a = TEXTFIELD_ATTR_RENAMES.get(attr.name.toLowerCase())) !== null && _a !== void 0 ? _a : attr.name;
            return attr.value ? `${result} ${name}="${attr.value}"` : `${result} ${name}`;
        }, '');
        recorder.insertRight(templateOffset + openTagEnd - 1, ` multi${textfieldAttrStr}`);
        const labelIndex = element.childNodes.findIndex((node) => node.nodeName === '#text' && (node === null || node === void 0 ? void 0 : node.value.trim()));
        let placeholderAttr = '';
        if (labelIndex !== -1) {
            const labelNode = element.childNodes[labelIndex];
            const labelText = labelNode.value.trim();
            const labelTextStart = ((_g = (_f = labelNode === null || labelNode === void 0 ? void 0 : labelNode.sourceCodeLocation) === null || _f === void 0 ? void 0 : _f.startOffset) !== null && _g !== void 0 ? _g : 0) + templateOffset;
            const labelTextEnd = ((_j = (_h = labelNode === null || labelNode === void 0 ? void 0 : labelNode.sourceCodeLocation) === null || _h === void 0 ? void 0 : _h.endOffset) !== null && _j !== void 0 ? _j : 0) + templateOffset;
            if (isLabelOutsideTrue) {
                recorder.remove(labelTextStart, labelTextEnd - labelTextStart);
                placeholderAttr = ` placeholder="${labelText}"`;
            }
            else if (!isDynamic) {
                recorder.insertRight(labelTextStart, '\n<label tuiLabel>');
                recorder.insertRight(labelTextEnd, '</label>\n');
            }
        }
        if (isDynamic) {
            const insertAt = ((_k = sourceCodeLocation === null || sourceCodeLocation === void 0 ? void 0 : sourceCodeLocation.startOffset) !== null && _k !== void 0 ? _k : 0) + templateOffset;
            recorder.insertLeft(insertAt, `<!-- ${insert_todo_1.TODO_MARK} [tuiTextfieldLabelOutside] is dynamic — cannot be migrated automatically. Use <label tuiLabel> inside <tui-textfield> for label-outside pattern.\n-->\n`);
        }
        if (todoAttrs.length > 0) {
            const lines = todoAttrs.map((attr, index, arr) => {
                const isLast = index === arr.length - 1;
                const hint = getHint(attr.name);
                return `     - ${attr.name}: ${hint}${isLast ? ' -->' : ''}`;
            });
            const todoComment = [
                `<!-- ${insert_todo_1.TODO_MARK} tui-input-tag migration (see ${DOCS_LINK}):`,
                ...lines,
            ].join('\n');
            const insertAt = ((_l = sourceCodeLocation === null || sourceCodeLocation === void 0 ? void 0 : sourceCodeLocation.startOffset) !== null && _l !== void 0 ? _l : 0) + templateOffset;
            recorder.insertLeft(insertAt, `${todoComment}\n`);
        }
        const insertOffset = ((_o = (_m = sourceCodeLocation === null || sourceCodeLocation === void 0 ? void 0 : sourceCodeLocation.endTag) === null || _m === void 0 ? void 0 : _m.startOffset) !== null && _o !== void 0 ? _o : 0) + templateOffset;
        const baseAttrs = [...controlAttrs, ...inputAttrs].reduce((result, attr) => {
            const nameLower = attr.name.toLowerCase();
            const renamed = INPUT_ATTR_RENAMES.get(nameLower);
            if (renamed !== undefined) {
                return attr.value
                    ? `${result} ${renamed}="${attr.value}"`
                    : `${result} ${renamed}`;
            }
            const original = (0, get_original_attr_text_1.getOriginalAttrText)(template, element, attr);
            return `${result} ${original}`;
        }, '');
        const migrationAttrs = `${baseAttrs}${(0, control_state_attrs_1.stringifyControlStateAttrs)(controlStateAttrs)}`;
        const itemChip = isLabelOutsideTrue ? '<tui-input-chip *tuiItem />\n' : '';
        recorder.insertRight(insertOffset, `\n<input tuiInputChip${migrationAttrs}${placeholderAttr} />\n${itemChip}`);
    });
}
function migrateSelfClosingInputTag({ element, template, recorder, templateOffset, resource, }) {
    var _a, _b;
    const loc = element.sourceCodeLocation;
    if (!(loc === null || loc === void 0 ? void 0 : loc.startTag)) {
        return;
    }
    const controlStateAttrs = (0, control_state_attrs_1.getControlStateAttrs)(element);
    const controlStateLower = new Set(controlStateAttrs.map((attr) => attr.name.toLowerCase()));
    const inputAttrParts = [];
    const wrapperAttrParts = [];
    const todoAttrs = [];
    let hasHintContent = false;
    let labelOutsideIsDynamic = false;
    let labelOutsideIsTrue = false;
    for (const attr of element.attrs) {
        const nameLower = attr.name.toLowerCase();
        if (controlStateLower.has(nameLower)) {
            continue;
        }
        if (HINT_ATTRS.has(nameLower)) {
            hasHintContent || (hasHintContent = nameLower === '[tuihintcontent]' || nameLower === 'tuihintcontent');
            continue;
        }
        if (LABEL_OUTSIDE_ATTRS.has(nameLower)) {
            const value = attr.value.trim();
            const isBinding = nameLower.startsWith('[');
            const isLabelOutsideTrue = value === 'true' || (!isBinding && value === '');
            labelOutsideIsTrue = isLabelOutsideTrue;
            labelOutsideIsDynamic =
                !isLabelOutsideTrue && value !== 'false' && value !== '';
            continue;
        }
        if (DROPPED_ATTRS.has(nameLower)) {
            continue;
        }
        if (TODO_ATTRS.has(nameLower)) {
            todoAttrs.push(attr);
            continue;
        }
        if (/formcontrol|ngmodel/.exec(nameLower)) {
            const original = (0, get_original_attr_text_1.getOriginalAttrText)(template, element, attr);
            inputAttrParts.push(original);
            continue;
        }
        const inputRename = INPUT_ATTR_RENAMES.get(nameLower);
        if (inputRename) {
            inputAttrParts.push(attr.value ? `${inputRename}="${attr.value}"` : inputRename);
            continue;
        }
        const textfieldRename = TEXTFIELD_ATTR_RENAMES.get(nameLower);
        if (textfieldRename) {
            wrapperAttrParts.push(attr.value ? `${textfieldRename}="${attr.value}"` : textfieldRename);
            continue;
        }
        wrapperAttrParts.push((0, get_original_attr_text_1.getOriginalAttrText)(template, element, attr));
    }
    const lineStart = template.lastIndexOf('\n', loc.startOffset) + 1;
    const indent = (_b = (_a = /^[ \t]*/.exec(template.slice(lineStart, loc.startOffset))) === null || _a === void 0 ? void 0 : _a[0]) !== null && _b !== void 0 ? _b : '';
    const wrapperStr = wrapperAttrParts.length > 0 ? ` ${wrapperAttrParts.join(' ')}` : '';
    const inputStr = inputAttrParts.length > 0 ? ` ${inputAttrParts.join(' ')}` : '';
    const controlStateStr = (0, control_state_attrs_1.stringifyControlStateAttrs)(controlStateAttrs);
    if (hasHintContent) {
        (0, add_import_to_closest_module_1.addImportToClosestModule)(resource.componentPath, 'TuiIcon', '@taiga-ui/core');
        (0, add_import_to_closest_module_1.addImportToClosestModule)(resource.componentPath, 'TuiTooltip', '@taiga-ui/kit');
    }
    const iconStr = hasHintContent ? (0, migrate_hint_on_legacy_controls_1.buildTuiIconStr)(element, template) : '';
    const iconLine = iconStr ? `${indent}${iconStr}\n` : '';
    const todoComment = buildSelfClosingTodo(todoAttrs, labelOutsideIsDynamic, indent);
    const itemChipLine = labelOutsideIsTrue
        ? `${indent}<tui-input-chip *tuiItem />\n`
        : '';
    const replacement = `${todoComment}<tui-textfield multi${wrapperStr}>\n` +
        `${indent}<input tuiInputChip${inputStr}${controlStateStr} />\n` +
        `${itemChipLine}${iconLine}${indent}</tui-textfield>`;
    recorder.remove(templateOffset + loc.startOffset, loc.startTag.endOffset - loc.startOffset);
    recorder.insertRight(templateOffset + loc.startOffset, replacement);
}
function buildSelfClosingTodo(todoAttrs, labelOutsideIsDynamic, indent) {
    const notes = [];
    if (labelOutsideIsDynamic) {
        notes.push('[tuiTextfieldLabelOutside] was dynamic and cannot be migrated automatically. Use <label tuiLabel> inside <tui-textfield> for the label-outside pattern.');
    }
    for (const attr of todoAttrs) {
        notes.push(`${attr.name}: ${getHint(attr.name)}`);
    }
    if (notes.length === 0) {
        return '';
    }
    const lines = [
        `<!-- ${insert_todo_1.TODO_MARK} tui-input-tag migration (see ${DOCS_LINK}):`,
        ...notes.map((note) => `     - ${note}`),
        '-->',
    ];
    return `${lines.join('\n')}\n${indent}`;
}
function getHint(attrName) {
    const lower = attrName.toLowerCase();
    if ('[tagValidator]'.toLowerCase() === lower) {
        return `use <tui-input-chip *tuiItem="let ctx" [appearance]="myValidator(ctx.item) ? '' : 'negative'"> inside <tui-textfield multi>. See ${DOCS_LINK}#customization`;
    }
    if ('[(search)]'.toLowerCase() === lower || '[search]'.toLowerCase() === lower) {
        return 'use (input) on <input tuiInputChip (input)="onSearch($any($event).target.value)"> to track changes; no direct equivalent for programmatic writes.';
    }
    if ('(searchChange)'.toLowerCase() === lower) {
        return 'use (input) on <input tuiInputChip (input)="onSearch($any($event).target.value)">.';
    }
    if ('[editable]'.toLowerCase() === lower || 'editable'.toLowerCase() === lower) {
        return `move to <tui-input-chip *tuiItem="let ctx" [editable]="..."> inside <tui-textfield multi>. See ${DOCS_LINK}.`;
    }
    return '[autoColor]'.toLowerCase() === lower || 'autoColor'.toLowerCase() === lower
        ? 'use tuiChip with auto-color appearance instead. See https://taiga-ui.dev/components/chip#auto-color'
        : `no direct equivalent in v5. See ${DOCS_LINK}.`;
}
//# sourceMappingURL=migrate-input-tag.js.map