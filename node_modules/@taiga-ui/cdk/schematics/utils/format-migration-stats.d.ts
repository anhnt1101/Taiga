export interface MigrationStepTiming {
    readonly name: string;
    readonly durationMs: number;
}
export declare function formatMigrationStats(steps: readonly MigrationStepTiming[], totalMs: number): string;
