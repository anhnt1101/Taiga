import { EventManagerPlugin } from '@angular/platform-browser';
export declare class LongtapEventPlugin extends EventManagerPlugin {
    private readonly isIOS;
    addEventListener(element: HTMLElement, _event: string, handler: (event: Event) => void): Function;
    supports(event: string): boolean;
    private listenContextmenuEvent;
    private listenTouchEvents;
    private dispatchLongtapEvent;
}
