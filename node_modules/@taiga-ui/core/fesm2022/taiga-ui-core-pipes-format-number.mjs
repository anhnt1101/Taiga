import * as i0 from '@angular/core';
import { inject, signal, computed, untracked, Pipe } from '@angular/core';
import { TUI_NUMBER_FORMAT } from '@taiga-ui/core/tokens';
import { tuiFormatNumber } from '@taiga-ui/core/utils/format';

class TuiFormatNumberPipe {
    constructor() {
        this.format = inject(TUI_NUMBER_FORMAT);
        this.value = signal(Number.NaN);
        this.settings = signal({});
        this.formatted = computed(() => tuiFormatNumber(this.value(), {
            ...this.format(),
            precision: Number.isNaN(this.format().precision)
                ? Infinity
                : this.format().precision,
            ...this.settings(),
        }));
    }
    /**
     * Formats number adding a thousand separators and correct decimal separator
     * padding decimal part with zeroes to given length
     * @param value number
     * @param settings See {@link TuiNumberFormatSettings}
     */
    transform(value, settings = {}) {
        untracked(() => {
            this.value.set(value);
            this.settings.set(settings);
        });
        return this.formatted();
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "19.2.25", ngImport: i0, type: TuiFormatNumberPipe, deps: [], target: i0.ɵɵFactoryTarget.Pipe }); }
    static { this.ɵpipe = i0.ɵɵngDeclarePipe({ minVersion: "14.0.0", version: "19.2.25", ngImport: i0, type: TuiFormatNumberPipe, isStandalone: true, name: "tuiFormatNumber", pure: false }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "19.2.25", ngImport: i0, type: TuiFormatNumberPipe, decorators: [{
            type: Pipe,
            args: [{
                    name: 'tuiFormatNumber',
                    pure: false,
                }]
        }] });

/**
 * Generated bundle index. Do not edit.
 */

export { TuiFormatNumberPipe };
//# sourceMappingURL=taiga-ui-core-pipes-format-number.mjs.map
