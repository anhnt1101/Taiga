import { InjectionToken, type Provider, type Signal } from '@angular/core';
import { type PolymorpheusContent } from '@taiga-ui/polymorpheus';
type ValidationValue = Record<string, PolymorpheusContent | Signal<PolymorpheusContent>>;
export declare const TUI_VALIDATION_ERRORS: InjectionToken<ValidationValue>;
export declare const tuiValidationErrorsProvider: (valueOrFactory: ValidationValue | (() => ValidationValue)) => Provider;
export {};
